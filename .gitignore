# Dependencies
node_modules/
/.pnp
.pnp.js

# Production
/build
/dist
/out

# Environment variables
.env
.env.local
.env.*.local

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Misc
coverage
.nyc_output

frontend/node_modules
frontend/.next
backend/node_modules
backend/dist
backend/.env
backend\.env
backend/*.env
backend/*.env.local
backend/uploads/
backend\uploads\


# Database dumps and backups (keep these out of the working tree;
# the VPS checkout must stay byte-identical to the repo)
*.dump
*.sql.gz
.env.backup
*.env.bak*
vps-local-changes-backup.patch

# Process / run artifacts that accumulate on the server
nohup.out
migration-result.json

# TypeScript incremental build cache - regenerated by every build,
# so tracking it guarantees drift between checkouts
frontend/tsconfig.tsbuildinfo
